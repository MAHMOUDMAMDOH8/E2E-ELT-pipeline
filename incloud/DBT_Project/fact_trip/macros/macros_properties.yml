version: 2

macros:
  - name: payment_type_mapping
    description: >
      This macro receives a payment_type and returns the corresponding description.
    arguments:
      - name: payment_type
        type: int
        description: > 
          payment_type value.
          Must be one of the accepted values, otherwise the macro will return null

  - name : rate_code_mapping
    description: >
      This macro receives a ratecodeID and returns the corresponding description.
    arguments:
      - name: RatecodeID
        type: int
        description: > 
          RatecodeID value.
          Must be one of the accepted values, otherwise the macro will return null

  - name : store_and_fwd_flag_mapping
    description: >
      This macro receives a store_and_fwd_flag and returns the corresponding description.
    arguments:
      - name: store_and_fwd_flag_mapping
        type: int
        description: > 
          store_and_fwd_flag value.
          Must be one of the accepted values, otherwise the macro will return null

  - name : trip_type_mapping
    description: >
      This macro receives a trip_typeID and returns the corresponding description.
    arguments:
      - name: trip_type
        type: int
        description: > 
          trip_type value.
          Must be one of the accepted values, otherwise the macro will return null

  - name : vendor_mapping
    description: >
      This macro receives a vendorID and returns the corresponding description.
    arguments:
      - name: vendorID
        type: int
        description: > 
          vendorID value.
          Must be one of the accepted values, otherwise the macro will return null